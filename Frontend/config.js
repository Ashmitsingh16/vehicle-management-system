// Set apiUrl to your HTTPS backend URL ending in /api when hosted separately.
// Empty uses localhost:5000 locally and same-origin /api in production.
window.APP_CONFIG = Object.freeze({ apiUrl: '' });
